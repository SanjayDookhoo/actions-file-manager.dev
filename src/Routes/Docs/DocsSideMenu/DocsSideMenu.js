import { cloneElement, useEffect } from 'react';
import { useLocation } from 'react-router-dom';
import DocsSideMenuItem from './DocsSideMenuItem';

const DocsSideMenu = ({ children }) => {
	return <>{children}</>;
};

export default DocsSideMenu;
